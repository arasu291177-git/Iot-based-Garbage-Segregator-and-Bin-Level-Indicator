# docker-compose.yml
version: '3.8'

services:
  waste-classifier:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./images:/app/images
      - ./models:/app/models
    restart: unless-stopped